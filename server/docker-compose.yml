services:
  server:
    build: .
    image: doctubeai-backend
    container_name: doctubeai-backend

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./uploaded_pdfs:/app/uploaded_pdfs

    restart: unless-stopped
