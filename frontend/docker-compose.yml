services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: doctubeai-frontend

    env_file:
      - .env

    ports:
      - "5173:5173"

    restart: unless-stopped